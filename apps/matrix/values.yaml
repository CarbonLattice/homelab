elementweb:
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
      external-dns.alpha.kubernetes.io/target: "homelab-tunnel.carbon-lattice.com"
      external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
    hosts:
      - host: &frontend_host chat.carbon-lattice.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: element-tls-certificate
        hosts:
          - *frontend_host
  config:
    default:
      base_url: https://matrix.carbon-lattice.com
      server_name: carbon-lattice.com

dendrite:
  dendrite_config:
    global:
      server_name: matrix.carbon-lattice.com
  ingress:
    enabled: true
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-prod
    hostName: matrix.carbon-lattice.com
    tls:
      - hosts:
          - matrix.carbon-lattice.com
        secretName: matrix-tls-certificate
  postgresql:
    enabled: true
